<div class="container">
	<h2>Title</h2>
		<%= @category.name if @category %>
	  <h3>Description:</h3>  
	  <%= @category.description if @category%>
</div>
<div class="container">
	<h3>Posts:</h3>
	<%= @post.title %>
</div>